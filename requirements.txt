# ── Interface grafica ──────────────────────────────────────────────────────
customtkinter>=5.2.2
Pillow>=10.4.0          # 10.3+ corrige varios CVEs de processamento de imagem

# ── Google Drive API ───────────────────────────────────────────────────────
google-api-python-client>=2.140.0
google-auth>=2.35.0     # 2.29+ corrige vulnerabilidade de validacao de token
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
googleapis-common-protos>=1.63.0

# ── Monitoramento de arquivos ──────────────────────────────────────────────
watchdog>=4.0.0

# ── Criptografia de credenciais ────────────────────────────────────────────
# cryptography >= 43 inclui correcoes de seguranca do OpenSSL
cryptography>=43.0.0

# ── Bandeja do sistema (Linux) ─────────────────────────────────────────────
pystray>=0.19.5

# ── Notificacoes desktop (opcional -- fallback para notify-send) ────────────
plyer>=2.1.0

# ── Utilitarios ───────────────────────────────────────────────────────────
# requests >= 2.32 corrige vulnerabilidade de URL (CVE-2024-35195)
requests>=2.32.0
